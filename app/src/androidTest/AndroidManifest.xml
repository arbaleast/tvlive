<?xml version="1.0" encoding="utf-8"?>
<!-- Test manifest: instrumentation targets main app package so tests
     run in the same process as the app under test. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <instrumentation
        android:name="androidx.test.runner.AndroidJUnitRunner"
        android:functionalTest="false"
        android:handleProfiling="false"
        android:label="Tests for com.example.netflixtv"
        android:targetPackage="com.example.netflixtv" />

</manifest>
